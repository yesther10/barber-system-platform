# OpenSpec Project Configuration — sistema-barberia
# Generated by sdd-init on 2026-07-31. Update as the stack is decided.

schema: spec-driven

context: |
  Project: sistema-barberia — online scheduling & management system for
  barbershops (client booking, barber profiles, services, appointments,
  admin panel).
  Stack: pnpm+Turborepo monorepo; Next.js (App Router) web + in-repo Node
  worker; TypeScript everywhere; Prisma/Postgres; Zod contracts shared;
  Vitest + Testcontainers + Playwright.
  Architecture: apps/{web,worker}, packages/{contracts,db,payments}; REST
  API-first; tenant isolation via barbershop_id.
  Testing: Vitest unit, Testcontainers integration, Playwright E2E.

testing:
  strict_tdd: true
  test_command: "vitest run"
  detected: 2026-08-02
  note: Strict TDD enabled after the Vitest toolchain was installed in the
        Bootstrap+CI work unit; test_command is vitest run at the repo root.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: true
    test_command: "vitest run"
  verify:
    test_command: "vitest run"
    build_command: "pnpm build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
